<%= simple_form_for [ @party_animal, @booking] do |f| %>
  <%= f.input :party_type %>

  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
 <%# eksik form!!!:city, :start_time, :end_time, :date, :total_price yok  %>
